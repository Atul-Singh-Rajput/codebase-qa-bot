

services:

  
  api:

   
    build: .

    
    container_name: codebase-qa-bot

    
    ports:
      - "8000:8000"

    
    env_file:
      - .env


    volumes:
      - ./chroma_store:/app/chroma_store
     

   
    restart: unless-stopped